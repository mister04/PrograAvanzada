@model IEnumerable<Proyecto_PrograAvanzada.Models2.TAB_TAGS>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TAGS)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TAGS)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_TAGS }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_TAGS }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_TAGS })
        </td>
    </tr>
}

</table>
